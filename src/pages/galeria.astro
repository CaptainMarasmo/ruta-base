---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout title="Galería | Ruta Base">
  <style>
    .wrap{ max-width: 1100px; margin: 0 auto; }

    .hero{
      display:grid;
      grid-template-columns: 1.15fr 0.85fr;
      gap: 18px;
      align-items: start;
    }

    .gallery{
      display:grid;
      gap: 14px;
      grid-template-columns: 1fr;
      justify-items: center;
    }

    .imgCard{
      border: 1px solid rgba(0,0,0,0.08);
      border-radius: 18px;
      overflow:hidden;
      background: rgba(255,255,255,0.75);
      box-shadow: 0 14px 28px rgba(0,0,0,0.10);
      transition: transform 180ms ease, box-shadow 180ms ease;
      width: 70%;
      transform-origin: center;
    }
    .imgCard:hover{
      transform: translateY(-2px);
      box-shadow: 0 18px 36px rgba(0,0,0,0.12);
    }

    /* Imagen normal (producto) */
    .img{
      width: 100%;
      height: 360px;
      object-fit: cover;
      display:block;
    }

    /* Card específica para foto vertical 9:16 */
    .imgCard--portrait{
      width: 70%;
      aspect-ratio: 9 / 16;
      display: grid;
      place-items: center;
      background: rgba(255,255,255,0.85);
    }

    /* Imagen vertical: que se vea entera */
    .img--portrait{
      width: 100%;
      height: 100%;
      object-fit: contain;
      display:block;
    }

    .panel{
      border: 1px solid rgba(0,0,0,0.08);
      border-radius: 18px;
      background: rgba(255,255,255,0.78);
      box-shadow: 0 14px 28px rgba(0,0,0,0.10);
      padding: 18px;
      position: sticky;
      top: 52px;
    }

    .badge{
      display:inline-flex;
      align-items:center;
      gap:8px;
      font-weight: 900;
      font-size: 12px;
      padding: 6px 10px;
      border-radius: 999px;
      background: rgba(155,120,255,0.22);
      border: 1px solid rgba(155,120,255,0.30);
      color:#1a0b2e;
    }

    h1{
      margin: 10px 0 6px;
      font-size: 34px;
      line-height: 1.06;
      letter-spacing: -0.6px;
      color:#1a0b2e;
    }

    .lead{
      margin: 0 0 14px;
      color:#3b2a57;
      font-size: 15px;
      line-height: 1.5;
    }

    .priceRow{
      display:flex;
      align-items: baseline;
      gap: 10px;
      margin: 12px 0 10px;
      padding: 12px;
      border-radius: 14px;
      background: rgba(255,255,255,0.55);
      border: 1px solid rgba(0,0,0,0.06);
    }

    .price{
      font-size: 34px;
      font-weight: 1000;
      color:#1a0b2e;
      letter-spacing: -0.6px;
    }

    .ship{
      color:#6b5b86;
      font-size: 13px;
      font-weight: 700;
    }

    .cta{
      display:flex;
      gap: 10px;
      margin-top: 10px;
      flex-wrap: wrap;
    }

    .btn{
      display:inline-flex;
      align-items:center;
      justify-content:center;
      gap:10px;
      height: 44px;
      padding: 0 14px;
      border-radius: 14px;
      border: 1px solid rgba(155,120,255,0.45);
      background: rgba(155,120,255,0.22);
      color:#1a0b2e;
      text-decoration:none;
      font-weight: 900;
      cursor: pointer;
      user-select:none;
      transition: transform 140ms ease, background 140ms ease;
    }
    .btn:hover{ transform: translateY(-1px); background: rgba(155,120,255,0.28); }

    .btn2{
      border: 1px solid rgba(0,0,0,0.10);
      background: rgba(255,255,255,0.55);
    }

    .features{
      margin-top: 12px;
      display:grid;
      gap: 10px;
    }
    .feat{
      display:flex;
      gap: 10px;
      align-items:flex-start;
      padding: 10px 12px;
      border-radius: 14px;
      border: 1px solid rgba(0,0,0,0.06);
      background: rgba(255,255,255,0.55);
    }
    .dot{
      width: 10px;
      height: 10px;
      border-radius: 999px;
      margin-top: 5px;
      background: rgba(90,40,160,0.60);
      flex: 0 0 auto;
    }
    .feat b{ color:#1a0b2e; }
    .feat span{ color:#5b4a75; font-size: 13px; line-height: 1.35; }

    .note{
      margin-top: 10px;
      color:#7a6a95;
      font-size: 12px;
      line-height: 1.4;
    }

    @media (max-width: 920px){
      .hero{ grid-template-columns: 1fr; }
      .panel{ position: relative; top: 0; }
      .img{ height: 300px; }
      .imgCard{ width: 100%; }
      .imgCard--portrait{ width: 100%; }
    }
  </style>

  <div class="wrap">
    <div class="hero">
      <div class="gallery">
        <!-- AZUL -->
        <div class="imgCard">
          <img class="img" src="/camisetaazul.png" alt="Camiseta Ruta Base azul" />
        </div>

        <div class="imgCard imgCard--portrait">
          <img class="img--portrait" src="/camisetaazulchica.png" alt="Camiseta Ruta Base azul (modelo)" />
        </div>

        <!-- GRIS -->
        <div class="imgCard">
          <img class="img" src="/camisetagris.png" alt="Camiseta Ruta Base gris" />
        </div>

        <div class="imgCard imgCard--portrait">
          <img class="img--portrait" src="/camisetagrischico.png" alt="Camiseta Ruta Base gris (modelo)" />
        </div>
      </div>

      <aside class="panel">
        <div class="badge">Edición Ruta Base · Azul & Gris</div>
        <h1>Camiseta Ruta Base</h1>
        <p class="lead">
          La camiseta para los que viajan con una idea clara: ruta, aventura y recuerdos.
          Sencilla, reconocible y con rollo “viajero”.
        </p>

        <div class="priceRow">
          <div class="price">23,50€</div>
          <div class="ship">Precio final</div>
        </div>

        <div class="cta">
          <a class="btn" href="#" onclick="alert('Aún no está configurado el pago. Cuando quieras, lo montamos con Stripe/PayPal.'); return false;">
            Comprar ahora
          </a>
          <a class="btn btn2" href="/sobre-mi">Sobre mí</a>
        </div>

        <div class="features">
          <div class="feat">
            <div class="dot"></div>
            <div>
              <b>Diseño Ruta Base</b><br />
              <span>Para llevar la marca contigo en cada viaje.</span>
            </div>
          </div>

          <div class="feat">
            <div class="dot"></div>
            <div>
              <b>Perfecta para carretera y vuelo</b><br />
              <span>Una prenda pensada para moverte.</span>
            </div>
          </div>

          <div class="feat">
            <div class="dot"></div>
            <div>
              <b>Regalo fácil</b><br />
              <span>Ideal para gente viajera de verdad.</span>
            </div>
          </div>
        </div>

        <div class="note">
          Nota: el botón “Comprar ahora” es de muestra. Si quieres venta real, dime si prefieres
          Stripe o PayPal y lo dejamos funcionando.
        </div>
      </aside>
    </div>
  </div>
</BaseLayout>





